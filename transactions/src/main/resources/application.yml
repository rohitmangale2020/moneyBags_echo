server:
  port: 8085
spring:
  application:
    name: transactions-service
  security:
    user:
      name: ${TRANSACTIONS_API_USERNAME:transactions-service}
      password: ${TRANSACTIONS_API_PASSWORD:local-development-only}
  datasource:
    url: jdbc:oracle:thin:@//ofss-mum-1212.snbomprshared1.gbucdsint02bom.oraclevcn.com:1521/pdb1
    username: ECHO
    password: Echo@123
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
services:
  accounts:
    base-url: ${ACCOUNTS_SERVICE_URL:http://localhost:8084}
    username: ${ACCOUNTS_SERVICE_USERNAME:accounts-service}
    password: ${ACCOUNTS_SERVICE_PASSWORD:local-development-only}
