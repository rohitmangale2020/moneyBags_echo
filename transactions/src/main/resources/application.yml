server:
  port: 8085
spring:
  application:
    name: transactions-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/transactions_db}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
