package com.training.platform.transactions.entity;

/** Defines the channels through which a transaction can be initiated. */
public enum TransactionChannel {
    MOBILE_APP,
    INTERNET_BANKING,
    BRANCH,
    ATM,
    API
}
