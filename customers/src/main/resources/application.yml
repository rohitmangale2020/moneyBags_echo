server:
  port: 8082
spring:
  application:
    name: customers-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/customers_db}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
